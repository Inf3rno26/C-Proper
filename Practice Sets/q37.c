#include <stdio.h>

int main()
{
    int number[5] = {1, 2, 3, 4, 5};
    // int sum = 0;

    int max = 0;
    for (int i = 0; i < 5; i++)
    {
        for 
    }

    // printf("Total Sum: %d", sum);
}